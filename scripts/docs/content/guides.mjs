/**
 * Authored prose for the narrative pages.
 *
 * Everything here is written by hand and reviewed like code. The reference
 * pages are generated from the source, so they cannot go stale; these pages
 * carry the reasoning, which no extractor can infer.
 */

export const overview = [
  {
    type: 'p',
    text: 'This is a **production-grade API automation framework** built on Playwright Test and TypeScript. It covers REST, GraphQL, Server-Sent Events, NDJSON streaming, WebSockets and SOAP/XML, with contract validation, seven authentication schemes, latency measurement, security auditing, a programmable stub server, and a reporting pipeline designed for pull requests rather than for a person watching a terminal.',
  },
  {
    type: 'p',
    text: 'It ships with **49 working tests across four real, public APIs** — a genuine create-read-update-delete lifecycle, `Link` header pagination, client verification against a neutral echo server, OpenAPI conformance, latency budgets and security auditing. `npm ci && npm test` produces a meaningful result on a fresh clone, with no credentials and no configuration.',
  },

  { type: 'h2', text: 'What this document is' },
  {
    type: 'p',
    text: 'A complete reference. **Every file in the repository has an entry** — what it is for, when you would need to change it, exactly how, and why the change belongs in that file rather than somewhere else. The build fails if a file has no entry, so this cannot quietly fall behind the code.',
  },
  {
    type: 'p',
    text: 'It is generated by `npm run docs`, which reads the TypeScript source with the compiler API, merges the extracted API surface with the prose in `scripts/docs/content/`, and emits this HTML site plus a PDF of the same material.',
  },

  { type: 'h2', text: 'How to read it' },
  {
    type: 'table',
    head: ['If you want to…', 'Go to'],
    rows: [
      [
        'See the framework actually working',
        '**Worked example** — the CRUD suite against a live API, explained line by line',
      ],
      [
        'Understand how the pieces fit together',
        '**Architecture** — layers, the path of a request, and why each boundary exists',
      ],
      [
        'Find the file that owns a concern',
        '**Project structure** — every file, grouped by role, linked to its entry',
      ],
      [
        'Change something',
        '**Playbooks** — thirty common tasks, each with the file, the steps and the reasoning',
      ],
      [
        'Know what a symbol does',
        '**API index** — every exported symbol, filterable, linked to its definition',
      ],
      [
        'Understand a failure',
        '**Troubleshooting** — failure modes, what causes them and how to diagnose them',
      ],
      [
        'Write a test somebody else can maintain',
        '**Conventions** — naming, tags, anti-patterns and the review checklist',
      ],
    ],
  },

  { type: 'h2', text: 'The ideas the framework is built on' },
  {
    type: 'p',
    text: 'Six decisions shape everything else. Each is argued where it applies, but they are worth stating up front because they explain why the code looks the way it does.',
  },
  {
    type: 'ol',
    items: [
      "**One place per concern.** The environment is read in exactly one file. Requests are sent in exactly one file. An endpoint's path is known to exactly one service object. Change is then a single edit, and the compiler finds everything affected.",
      '**A response is a snapshot, not a stream.** The body is captured into a buffer before the response object exists, so it can be read as text, JSON, NDJSON, XML or bytes, in any order, as many times as a test likes — and every assertion can be synchronous and chainable.',
      '**Failures must explain themselves.** Every assertion reports the request, the status, the timing and a body excerpt. An error message is usually all anyone sees in CI, and a message that costs nothing to write saves somebody twenty minutes.',
      '**Retry transport faults, never verdicts.** A connection reset is worth retrying; a 422 is an answer. The two are separated structurally in the client, not by convention.',
      "**Generate data, deterministically.** Faker is seeded from each test's own title, so data is unique between tests and identical between runs — random enough to catch collisions, reproducible enough to debug.",
      '**Clean up at the point of creation.** A service method that creates a resource registers its own deletion in the same statement. Cleanup that depends on somebody remembering is cleanup that does not happen.',
    ],
  },

  { type: 'h2', text: 'What is in the box' },
  {
    type: 'table',
    head: ['Area', 'Capability'],
    rows: [
      [
        'Request',
        'Fluent builder: path params, query with three array encodings, six body encodings, per-request timeout, retries, accepted statuses, idempotency keys, anonymous mode, redirect control',
      ],
      [
        'Transport',
        'One engine: credential injection, exponential backoff with jitter and `Retry-After`, read-only guard, timing capture, reporter steps, exchange listeners',
      ],
      [
        'Response',
        'JSON / NDJSON / XML / text / binary readers, JSON-path navigation, Zod parsing, 15 assertion methods, soft mode, report attachment',
      ],
      [
        'Auth',
        'None, Basic, Bearer, API key (header or query), OAuth2 client-credentials / password / refresh, HMAC request signing, cookie session, JWT decoding',
      ],
      [
        'Contracts',
        'Zod schema library, JSON Schema via Ajv, OpenAPI conformance with `$ref` resolution, a path-pattern registry, and automatic enforcement under `STRICT_CONTRACTS`',
      ],
      [
        'Protocols',
        'GraphQL (queries, mutations, batching, introspection, error assertions), SSE, NDJSON streaming, WebSocket with pre-buffering',
      ],
      [
        'Testing support',
        'Stub server with delays, failures and controlled flakiness; exchange recorder for offline replay; latency percentiles; structural diffing; security audits; pagination walkers; polling',
      ],
      [
        'Reporting',
        'HTML, JUnit, JSON, CTRF, optional Allure, and a custom machine-readable run summary',
      ],
    ],
  },

  { type: 'h2', text: 'Getting started' },
  {
    type: 'code',
    caption: 'First run — no configuration required',
    text: `npm ci
npm test`,
  },
  {
    type: 'p',
    text: 'That is the whole setup. Copy `.env.example` to `.env` only when you point the framework at your own API.',
  },
  {
    type: 'code',
    caption: 'Useful selections',
    text: `npx playwright test --grep-invert @live   # everything that spends no API quota
npx playwright test --project=contract    # offline: no network at all
npx playwright test --grep @smoke         # the pre-deploy subset`,
  },
  {
    type: 'p',
    text: 'Then read the **Worked example**, then **Architecture**, then write your first spec following **Conventions**. `make` lists every other command.',
  },

  { type: 'h2', text: 'What it is tested against' },
  {
    type: 'p',
    text: 'Four real services, each chosen for what it alone can prove.',
  },
  {
    type: 'table',
    head: ['API', 'Proves', 'Limits'],
    rows: [
      [
        '`api.restful-api.dev`',
        'A real CRUD lifecycle — the only one of the four that genuinely persists writes',
        '**50 anonymous requests / 24 h**; the live suite skips rather than fails',
      ],
      [
        '`jsonplaceholder.typicode.com`',
        'Real `Link` header pagination over a stable 100-record dataset',
        'None. Writes are simulated',
      ],
      ['`httpbin.org`', 'That the client puts on the wire what it claims to', 'None'],
      [
        'The built-in stub server',
        'The same lifecycle offline, plus failure modes no real API produces on demand',
        'None',
      ],
    ],
  },
  {
    type: 'note',
    text: 'There is no free, unauthenticated API offering both genuine persistence and an unlimited quota — several were tried and rejected, and the **Worked example** records which and why. So the CRUD suite is split: a small live suite proves the API behaves as we believe, and an unlimited stubbed suite runs the same service object against an executable model of the same contract on every run.',
  },
];

/* ------------------------------------------------------------------ */

export const architecture = [
  {
    type: 'p',
    text: 'The framework is a stack of layers, each of which knows only about the layer beneath it. A test at the top can reach down as far as it needs, but nothing reaches upward — which is what makes any single layer replaceable without touching the others.',
  },

  { type: 'h2', text: 'The layers' },
  {
    type: 'tree',
    text: `  tests/                 what should be true
    ↓
  src/fixtures/          the test and expect a spec imports; wiring and lifecycle
    ↓
  src/services/          one object per resource: paths, payloads, domain methods
    ↓
  src/core/              the request builder, the HTTP engine, the response wrapper
    ↓  ↘
    │    src/auth/         credentials, injected per request
    │    src/contracts/    schemas, JSON Schema, OpenAPI conformance
    │    src/protocols/    GraphQL, SSE, NDJSON, WebSocket
    │    src/mocks/        stub server and exchange recorder
    ↓
  src/config/            the only reader of process.env, validated at start-up
    ↓
  Playwright APIRequestContext → the network`,
  },

  { type: 'h2', text: 'The path of a request' },
  {
    type: 'p',
    text: 'Following one call all the way down is the fastest way to understand the framework. This is what happens when a test writes `await api.users.create()`.',
  },
  {
    type: 'steps',
    items: [
      {
        text: "**The fixture builds the client.** `src/fixtures/api.fixture.ts` constructs an `HttpClient` bound to Playwright's `request` context and to whichever auth provider the environment implies, then attaches the latency collector and the exchange recorder to it.",
      },
      {
        text: '**The service composes the request.** `UserService.create` builds a payload from the seeded factory and describes the call — path, body, idempotency key, acceptable status — through the fluent builder. Nothing has been sent yet.',
        code: `const response = await this.post()\n  .json(payload)\n  .idempotencyKey()\n  .expectStatus(201)\n  .as('create user')\n  .send();`,
      },
      {
        text: '**The builder resolves the request.** `RequestBuilder.build()` fills path placeholders, encodes the query, decides the content type from the body kind, and returns an immutable `RequestSpec`. A placeholder left unfilled fails here, with a message naming it.',
      },
      {
        text: "**The client applies every cross-cutting policy.** `HttpClient.dispatch` refuses mutating verbs on a read-only environment, opens a reporter step, merges credentials from the auth provider under the request's own headers, and sends.",
      },
      {
        text: '**The transport runs, and only the transport is retried.** A connection failure or a retryable status backs off — honouring `Retry-After` when the server sent one — and tries again. Everything after a response has arrived sits outside that catch, so a schema violation is never retried.',
      },
      {
        text: '**The response is captured whole.** Status, headers and the complete body buffer become a `ResponseSnapshot`, the underlying response is disposed, and an `ApiResponse` wraps it.',
      },
      {
        text: '**Observers see it.** The latency collector records the timing, the recorder stores a redacted copy, and — when `STRICT_CONTRACTS` is on — the contract guard validates the payload against its registered schema and throws if it disagrees.',
      },
      {
        text: '**The service returns a domain value.** `response.parse(UserSchema)` validates and narrows the type, the deletion is registered with the cleanup registry, and the test receives a fully typed `User`.',
        code: `const user = response.parse(UserSchema, 'user');\nreturn this.track(user, \`user \${user.id}\`, () => this.remove(user.id));`,
      },
    ],
  },

  { type: 'h2', text: 'Why a builder rather than an options object' },
  {
    type: 'p',
    text: 'A request has more than a dozen independent dimensions. Expressed as one options object, every call site has to be read in full to find the two fields that matter, and adding a thirteenth dimension is a breaking change for everybody. A builder lets each call name only what it cares about, in any order, and lets a new capability be added without touching a single existing test.',
  },
  {
    type: 'p',
    text: "The builder is also **thenable**, which is what keeps the simple case simple: `await http.get('/users')` sends the request, while `await http.get('/users').query({ page: 2 }).timeout(5000)` sends a different one. Nothing is sent until the builder is awaited.",
  },

  { type: 'h2', text: 'Why the response is a snapshot' },
  {
    type: 'p',
    text: 'Most HTTP clients hand back a stream that can be consumed once. That forces every test to decide up front whether it wants text or JSON, makes a second look at the body impossible, and turns every assertion into an `await`.',
  },
  {
    type: 'p',
    text: 'Capturing the body into a buffer before the wrapper exists removes all three problems at once. It costs one buffer per request — irrelevant for an API suite — and buys synchronous, chainable, re-readable assertions.',
  },
  {
    type: 'code',
    caption: 'What the snapshot makes possible',
    text: `const response = await http.get('/report').send();

response
  .expectOk()
  .expectContentType('application/json')
  .expectPath('summary.total', 42)
  .expectFasterThan(500);

const rows = response.ndjson<Row>();        // same body, different reader
const raw = response.text();                // and again
const checksum = createHash('sha256').update(response.buffer()).digest('hex');`,
  },

  { type: 'h2', text: 'Why service objects' },
  {
    type: 'p',
    text: 'A service object is to an API suite what a page object is to a UI suite. Its value is not abstraction for its own sake — it is that when `/v1/users` becomes `/v1/accounts`, exactly one file changes rather than every test that happened to mention users.',
  },
  {
    type: 'rule',
    text: 'A service method returns a domain value and never asserts. Assertions belong in tests, so a negative-path test can reuse the same service instead of working around it. Where a test genuinely needs the response — a `Location` header, a status, a timing — the service exposes a `raw*` method rather than forcing the test to bypass it.',
  },

  { type: 'h2', text: 'Fixtures and scope' },
  {
    type: 'p',
    text: 'Playwright constructs a fixture only when a test names it, so the fixture file can grow indefinitely without slowing anything down: a test that never mentions `socket` never opens a WebSocket.',
  },
  {
    type: 'table',
    head: ['Scope', 'Built', 'Use for', 'Examples here'],
    rows: [
      [
        'worker',
        'once per worker process',
        'expensive, shareable, effectively immutable things',
        '`tokens`, `workerRequest`, `mockServer`, `contract`',
      ],
      [
        'test',
        'once per test',
        'anything a test can mutate, and anything that must not leak between tests',
        '`http`, `api`, `cleanup`, `data`, `latency`, `recorder`',
      ],
    ],
  },
  {
    type: 'warn',
    text: 'Putting mutable state in worker scope is how a suite becomes order-dependent: it passes locally with one worker and fails in CI with eight, and the failure moves every run. If a test can change it, it belongs in test scope.',
  },

  { type: 'h2', text: 'Authentication' },
  {
    type: 'p',
    text: 'Every provider implements one interface — `headers(): Promise<HeaderMap>` — called per request, so a token that refreshes mid-run is picked up without rebuilding anything. `defaultAuthProvider` chooses between them from what the environment supplies, in order: OAuth2 if a token endpoint is configured, then an API key, then Basic credentials, then nothing.',
  },
  {
    type: 'p',
    text: 'That ordering is what makes moving an environment from API keys to OAuth a `.env` change rather than a code change.',
  },
  {
    type: 'p',
    text: 'Tokens are cached in a `TokenStore` keyed by grant and subject, and are treated as expired thirty seconds early. Both details matter: a token endpoint is usually rate-limited harder than the API, and a token that expires mid-flight produces a 401 nobody can reproduce.',
  },

  { type: 'h2', text: 'Contracts' },
  { type: 'p', text: 'Three mechanisms, because they answer three different questions.' },
  {
    type: 'table',
    head: ['Mechanism', 'Answers', 'Where'],
    rows: [
      [
        'Zod schemas',
        '"Is this the shape the test expects?" — and gives the test a type as a by-product',
        '`src/contracts/schemas.ts`, and each service',
      ],
      [
        'JSON Schema (Ajv)',
        '"Does this satisfy a schema somebody else published?"',
        '`src/contracts/json-schema.ts`',
      ],
      [
        'OpenAPI conformance',
        '"Does the API still match its own published document?"',
        '`src/contracts/openapi.ts`',
      ],
    ],
  },
  {
    type: 'p',
    text: 'The registry ties them to traffic. A schema registered against `GET /users/{id}` is found for a real request to `/v1/users/42`, and with `STRICT_CONTRACTS=true` every matching response is validated whether or not the test asked. That is on in CI and off locally, so a work-in-progress schema does not block a developer but drift cannot reach main.',
  },

  { type: 'h2', text: 'Projects' },
  {
    type: 'p',
    text: '`playwright.config.ts` defines five projects. They exist because these groups of tests have genuinely different requirements, not to tidy the file listing.',
  },
  {
    type: 'table',
    head: ['Project', 'Why it is separate'],
    rows: [
      [
        '`setup`',
        "Authenticates once and writes the session to `storage/`. Everything else declares `dependencies: ['setup']`, so nothing runs unauthenticated.",
      ],
      ['`api`', 'The functional suite. Fully parallel.'],
      [
        '`contract`',
        'Answers a different question — "does this still match what we published" — and usually runs on a different trigger. It can run against the stub server with no environment at all.',
      ],
      [
        '`performance`',
        'One worker, no retries. A percentile measured while eight workers hammer the same endpoint measures the load, not the endpoint.',
      ],
      [
        '`security`',
        'Deliberately hostile payloads. Separated so a pipeline can point it at a dedicated environment and never at production.',
      ],
    ],
  },

  { type: 'h2', text: 'What a run leaves behind' },
  {
    type: 'table',
    head: ['Artefact', 'Path', 'Audience'],
    rows: [
      ['HTML report', '`reports/html/`', 'A person investigating a failure'],
      ['Run summary', '`reports/summary.json`', 'A pipeline gate, or a chat message'],
      ['JUnit XML', '`reports/junit/results.xml`', 'CI test-result panels'],
      ['CTRF JSON', '`reports/ctrf/`', 'Cross-tool result aggregation'],
      ['Recordings', '`reports/recordings/`', 'Failed tests only — the full exchange, redacted'],
      ['Traces', '`test-results/`', 'Every request and response, replayable'],
    ],
  },

  { type: 'h2', text: 'Testing against an API you cannot call freely' },
  {
    type: 'p',
    text: 'The CRUD target allows 50 anonymous requests a day. That constraint produced a pattern worth generalising, because most teams meet some version of it — a rate limit, a shared sandbox, a dependency that is down half the time.',
  },
  {
    type: 'table',
    head: ['Suite', 'Runs against', 'Proves', 'How often'],
    rows: [
      [
        '`tests/api/objects.crud.spec.ts`',
        'The live API',
        'The API really behaves as we believe',
        'A few times a day, within quota',
      ],
      [
        '`tests/contract/objects.stubbed.spec.ts`',
        '`src/mocks/objects.stub.ts`',
        'Our client handles that behaviour correctly',
        'Every run, offline, unlimited',
      ],
    ],
  },
  {
    type: 'p',
    text: 'Both drive **the same `ObjectService`**. That is what makes the arrangement work: the stub is not a parallel implementation to keep in step, it is a server the real client talks to.',
  },
  {
    type: 'rule',
    text: 'A model nobody checks against reality is a fixture that encodes an assumption and then defends it forever. The live suite is what keeps the model honest; the model is what makes the coverage affordable. Neither is sufficient alone.',
  },
  {
    type: 'p',
    text: 'The stub also buys something the live API cannot give at any quota: failure modes on demand. A real service will not produce a 503, a two-second delay or a malformed body when you ask it to, so those paths would otherwise never be tested.',
  },

  { type: 'h2', text: 'Deliberate omissions' },
  {
    type: 'p',
    text: 'Things that were considered and left out, so nobody has to re-derive the reasoning.',
  },
  {
    type: 'ul',
    items: [
      '**No load testing.** Latency percentiles from a functional suite are useful signal; pretending they are a load test is not. Use a tool built for it.',
      "**No JWT signature verification.** Verifying needs the issuer's key and is the API's job. The suite decodes claims — it never treats a decoded token as trusted.",
      "**No external `$ref` resolution in OpenAPI.** Bundle the specification with your own tooling first; a second resolver that behaves slightly differently from the team's is worse than none.",
      '**No authorisation-code OAuth flow.** It needs a browser. The UI suite can perform it and hand the resulting token to `.bearer(token)` on the request builder.',
      '**No assertion retrying.** `retry()` exists for genuinely transient work. Retrying until an assertion passes is how a real defect gets shipped.',
    ],
  },
];

/* ------------------------------------------------------------------ */

export const workedExample = [
  {
    type: 'p',
    text: 'A line-by-line reading of the CRUD suite in `tests/api/objects.crud.spec.ts`, which runs against **https://api.restful-api.dev** — a live public API that genuinely persists writes. Every call is explained: what it does, why it is there, and what would go wrong without it.',
  },
  {
    type: 'p',
    text: 'If you read one page of this document, read this one. Everything else is reference; this is the framework doing its job.',
  },

  { type: 'h2', text: 'The APIs, and why each was chosen' },
  {
    type: 'p',
    text: 'Finding a suitable target was itself a piece of work worth recording. **There is no free, unauthenticated public API that offers both genuine persistence and an unlimited quota.** Several were tried and rejected:',
  },
  {
    type: 'table',
    head: ['Candidate', 'Outcome'],
    rows: [
      [
        '`api.restful-api.dev`',
        '**Chosen.** Real persistence, no auth — but 50 anonymous requests per 24 hours.',
      ],
      [
        '`jsonplaceholder.typicode.com`',
        '**Chosen for reads.** Unlimited and stable, with real `Link` header pagination — but writes are simulated and store nothing.',
      ],
      [
        '`httpbin.org`',
        '**Chosen for client verification.** Echoes back exactly what it received.',
      ],
      ['`petstore3.swagger.io`', 'Rejected — its demo instance answers 500 to every write.'],
      ['`api.escuelajs.co`', 'Rejected — 500 on create.'],
      ['`dummyjson.com`', 'Rejected — writes are simulated; a created id 404s on read.'],
      [
        '`gorest.co.in`',
        'Rejected — writes need a bearer token, so it cannot run on a fresh clone.',
      ],
    ],
  },
  {
    type: 'p',
    text: 'So the suite is split. The live file proves the API behaves as we believe, a few times a day. `tests/contract/objects.stubbed.spec.ts` runs the **same service object** against an executable model of the same contract, unlimited and offline, on every run. If the two disagree, either the API changed or we misread it.',
  },

  { type: 'h2', text: 'Step 1 — the imports' },
  {
    type: 'code',
    caption: 'tests/api/objects.crud.spec.ts',
    text: `import { test, expect } from '../../src/fixtures';
import { uniqueId } from '../../src/utils/data.utils';
import {
  ApiObjectSchema,
  CreatedObjectSchema,
  DeleteAcknowledgementSchema,
} from '../../src/services/object.service';`,
  },
  {
    type: 'rule',
    text: '`test` and `expect` come from `../../src/fixtures`, **never** from `@playwright/test`. The Playwright versions have no cleanup registry, no contract guard and none of the custom matchers — and nothing will tell you. This is the single most expensive mistake available in the repository.',
  },
  {
    type: 'p',
    text: 'The schemas are imported from the service rather than redefined, so the test and the service can never disagree about what a valid object looks like.',
  },

  { type: 'h2', text: 'Step 2 — the quota guard' },
  {
    type: 'code',
    caption: 'Skipping is not failing',
    text: `let quotaProbe: Promise<boolean> | undefined;

test.beforeEach(async ({ api }) => {
  quotaProbe ??= api.objects.quotaAvailable();

  test.skip(
    !(await quotaProbe),
    'The restful-api.dev anonymous quota (50 requests / 24h) is exhausted. ' +
      'The same lifecycle is covered offline by tests/contract/objects.stubbed.spec.ts.',
  );
});`,
  },
  {
    type: 'ul',
    items: [
      '**Why a module-level variable?** The probe costs a request. Caching it means one probe per worker process rather than one per test.',
      '**Why cache the promise, not the result?** Concurrent tests in the same worker then share a single in-flight request instead of racing to make several.',
      "**Why skip rather than fail?** A red build caused by somebody else having spent the day's quota teaches nobody anything, and a suite people learn to ignore is worse than no suite.",
      '**Why `??=`?** It assigns only when the variable is still undefined — the concise form of "memoise this".',
    ],
  },

  { type: 'h2', text: 'Step 3 — generating the payload' },
  {
    type: 'code',
    text: `function deviceFor(label: string): { name: string; data: Record<string, unknown> } {
  return {
    name: \`\${label} \${uniqueId('pw')}\`,
    data: {
      year: 2026,
      price: 1849.99,
      'CPU model': 'Apple M4 Pro',
      'Hard disk size': '1 TB',
    },
  };
}`,
  },
  {
    type: 'ul',
    items: [
      "`uniqueId('pw')` appends a timestamp and random suffix, so two parallel workers cannot produce the same name. A hard-coded name would collide the moment the suite ran twice.",
      'The `data` keys include one containing a **space**. That is a shape this API really accepts, and a useful check that nothing in the framework mangles keys on the way out.',
    ],
  },

  { type: 'h2', text: 'Step 4 — CREATE' },
  {
    type: 'code',
    caption: 'The test',
    text: `const created = await test.step('create the object', async () => {
  const object = await api.objects.create(device);

  expect(object.id, 'the API must assign an id').toBeTruthy();
  expect(object.name).toBe(device.name);
  expect(object.data).toMatchObject(device.data);
  expect(object.createdAt).toBeGreaterThan(Date.now() - 120_000);

  return object;
});`,
  },
  {
    type: 'code',
    caption: 'And what the service does underneath',
    text: `const response = await this.post()
  .json({ name: input.name, data: input.data ?? null })
  .idempotencyKey()
  .expectStatus(200)
  .as('create object')
  .send();

const created = response.parse(CreatedObjectSchema, 'object-created');
return this.track(created, \`object \${created.id}\`, () => this.remove(created.id));`,
  },
  {
    type: 'table',
    head: ['Call', 'What it does', 'What breaks without it'],
    rows: [
      ['`this.post()`', 'A builder for `POST /objects`, pre-labelled for the report', ''],
      ['`.json(...)`', 'JSON body, `Content-Type: application/json` set automatically', ''],
      [
        '`.idempotencyKey()`',
        'Generates a key so the POST is safe to retry',
        'The client refuses to retry a POST at all — a transient 503 on a create becomes a hard failure',
      ],
      [
        '`.expectStatus(200)`',
        'Enforces the status the API **really** returns',
        'Expecting 201 would raise on every single create',
      ],
      [
        '`.as(...)`',
        'Names the call in the report, logs and recording',
        'Failures identifiable only by URL',
      ],
      ['`.send()`', 'Issues the request; nothing happens until now', ''],
      [
        '`.parse(schema)`',
        'Validates **and narrows** the type',
        '`json<T>()` would assert a shape rather than check one',
      ],
      [
        '`this.track(...)`',
        'Registers the deletion at the moment of creation',
        'A leaked object per run, found by somebody else months later',
      ],
    ],
  },
  {
    type: 'note',
    text: 'Asserting the echo — that `name` and `data` came back as sent — matters more than it looks. An API that silently drops a field on write is a real defect, and a create test that only checked the status would never see it.',
  },
  {
    type: 'p',
    text: 'The `createdAt` check proves the server generated a timestamp *now* rather than echoing something stale. It is epoch milliseconds, and it appears on the create response and nowhere else — a quirk the schemas encode by splitting `CreatedObject`, `Object` and `UpdatedObject` into three.',
  },

  { type: 'h2', text: 'Step 5 — READ, and why it is not optional' },
  {
    type: 'code',
    text: `const fetched = await api.objects.require(created.id);

expect(fetched.id).toBe(created.id);
expect(fetched.name).toBe(created.name);
expect(fetched.data).toMatchObject(device.data);
expect(fetched, 'reads carry no createdAt').not.toHaveProperty('createdAt');`,
  },
  {
    type: 'rule',
    text: 'The read is what proves the write actually persisted. Without it, a create test passes just as happily against an API that accepts the request and throws it away.',
  },
  {
    type: 'p',
    text: '`require` is the throwing form; `find` returns `undefined` for a 404 so a caller can branch on absence. Having both means neither a `try`/`catch` nor a null check is ever forced on the test.',
  },
  {
    type: 'p',
    text: 'The final assertion records a real quirk rather than glossing over it: reads carry **no timestamps at all**, even though the create carried `createdAt`. A single schema requiring it would fail on every read — which is exactly the mistake a schema copied from documentation would make.',
  },

  { type: 'h2', text: 'Step 6 — REPLACE, and the assertion most people forget' },
  {
    type: 'code',
    text: `const replaced = await api.objects.replace(created.id, {
  name: \`MacBook Pro M5 \${uniqueId('pw')}\`,
  data: { year: 2027, price: 2199.99, 'CPU model': 'Apple M5 Max' },
});

expect(replaced.updatedAt).toBeGreaterThan(0);

const afterReplace = await api.objects.require(created.id);
expect(afterReplace.data).not.toHaveProperty('Hard disk size');`,
  },
  {
    type: 'p',
    text: 'The last line is the whole point of testing PUT. A full replacement must **discard** what it was not sent — `Hard disk size` was present before and absent from the replacement, so it must be gone rather than merged. A test that only asserted the status, or only that the new fields arrived, would pass against a server that quietly merged instead.',
  },
  {
    type: 'p',
    text: '`updatedAt` appears here and not on creates — the mirror image of the `createdAt` quirk, and the reason `UpdatedObjectSchema` exists.',
  },

  { type: 'h2', text: 'Step 7 — PATCH, the other half of that distinction' },
  {
    type: 'code',
    text: `const patched = await api.objects.update(created.id, { name: patchedName });

expect(patched.name).toBe(patchedName);
expect(patched.data, 'PATCH must not clear untouched fields').toMatchObject({
  'CPU model': 'Apple M5 Max',
});`,
  },
  {
    type: 'p',
    text: 'Only `name` was sent, so `data` must survive. That is precisely what distinguishes PATCH from PUT, and precisely what a test asserting only the status would fail to check. The two assertions together — PUT discards, PATCH preserves — are what make the pair meaningful.',
  },

  { type: 'h2', text: 'Step 8 — DELETE, and the follow-up read' },
  {
    type: 'code',
    text: `await api.objects.remove(created.id);

const afterDelete = await api.objects.find(created.id);
expect(afterDelete, 'the object must be gone after DELETE').toBeUndefined();`,
  },
  {
    type: 'p',
    text: 'A delete test without the follow-up read proves only that the API accepted the request, which is not the same thing as the resource being gone.',
  },
  {
    type: 'code',
    caption: 'And the service call that makes cleanup safe',
    text: `await this.del('/{id}').param('id', id).expectStatus(200, 404).send();
this.cleanup.forget(\`object \${id}\`);`,
  },
  {
    type: 'p',
    text: '404 is accepted because `remove` also runs from the cleanup registry, after tests that already deleted their own object. If a repeated delete failed, every such test would go red in teardown for no reason. `forget` then removes the registration so teardown does not try a third time.',
  },

  { type: 'h2', text: 'What the framework did that the test never mentions' },
  {
    type: 'table',
    head: ['Happened automatically', 'Where it comes from'],
    rows: [
      ['A client was built and authenticated for the environment', 'the `http` fixture'],
      ['Every request became a step in the HTML report', '`HttpClient.dispatch`'],
      [
        'Every request and response was timed and bucketed by route',
        'the `latency` fixture, attached as `latency.json`',
      ],
      [
        'Every exchange was recorded, redacted, and saved **only because a failure would need it**',
        'the `recorder` fixture',
      ],
      [
        'Every response was validated against its registered schema when `STRICT_CONTRACTS=true`',
        'the `contractGuard` auto fixture',
      ],
      [
        'The created object was deleted, even if the test failed',
        'the `cleanup` fixture draining the registry',
      ],
      [
        'A transient 503 would have been retried with backoff; a 404 would not',
        '`HttpClient.execute`',
      ],
      [
        'A mutating verb against a read-only environment would have been refused before sending',
        '`guardReadOnly`',
      ],
    ],
  },
  {
    type: 'p',
    text: 'That list is the argument for a framework rather than a folder of scripts. None of it appears in the test, none of it can be forgotten, and all of it applies to the next test somebody writes.',
  },

  { type: 'h2', text: 'Two real bugs this work found' },
  { type: 'h3', text: '1. Text bodies were being re-encoded' },
  {
    type: 'p',
    text: "A test sending a deliberately malformed body — to prove the API rejects it — was passing for the wrong reason. Playwright re-serialises a **string** `data` when the content type is JSON, so `.text('{ not json', 'application/json')` arrived at the server as the valid JSON string `\"{ not json\"`.",
  },
  {
    type: 'p',
    text: 'The stub could not see it, because the stub received exactly what the transport chose to send. Only the neutral echo server could. The fix, in `http.client.ts`, is to send text as a Buffer, which is written verbatim:',
  },
  {
    type: 'code',
    text: `case 'text':
  options.data = Buffer.from(spec.text ?? '', 'utf8');
  break;`,
  },
  { type: 'h3', text: '2. A flaky assertion of my own making' },
  {
    type: 'p',
    text: 'A security test searched a whole response body for `49` — the result of `{{7*7}}` had the template been evaluated. It passed alone and failed in the full run, because httpbin echoes numeric headers such as `Content-Length` and the substring appeared for entirely unrelated reasons. Asserting on the specific field instead is both stricter and stable.',
  },
  {
    type: 'rule',
    text: 'A flaky test deserves a diagnosis, not a retry. Both of these looked like environment noise and were not.',
  },

  { type: 'h2', text: 'Adapting this to your own API' },
  {
    type: 'steps',
    items: [
      {
        text: 'Add your environment to `src/config/environments.ts` — URLs, prefix, latency budget, read-only flag. No secrets.',
      },
      {
        text: 'Copy `src/services/object.service.ts`. Declare the schema first and infer the type from it, so the runtime check and the compile-time type cannot disagree.',
        code: `export const OrderSchema = z.object({ id: identifier, status: z.enum(['draft', 'paid']) });
export type Order = z.infer<typeof OrderSchema>;`,
      },
      {
        text: 'Register the schemas so the contract guard can find them from a real request.',
        code: `registerSchemas([
  { name: 'order', method: 'GET', pathPattern: '/orders/{id}', status: '2xx', schema: OrderSchema },
]);`,
      },
      {
        text: 'Add the service to the `api` fixture in `src/fixtures/api.fixture.ts` — one line, and every test can reach it.',
      },
      {
        text: 'Copy the shape of `objects.crud.spec.ts`: one composing lifecycle test, several focused ones. The lifecycle tells you *that* something broke; the focused tests tell you *what*.',
      },
      {
        text: 'If your API has a quota or is otherwise unreliable, model it in `src/mocks/` and run the same service against both.',
      },
    ],
  },
];

/* ------------------------------------------------------------------ */

export const playbooks = [
  {
    type: 'p',
    text: 'Each entry below is a task somebody will need to do, with **where to go, what to do, and why it belongs there**. They are ordered roughly by how often they come up.',
  },

  { type: 'h2', text: 'Run the suite without spending API quota' },
  {
    type: 'p',
    text: '**Where:** the command line. The CRUD target allows 50 anonymous requests per 24 hours, and every test that spends them is tagged `@live`.',
  },
  {
    type: 'code',
    caption: 'Three levels of thrift',
    text: `npx playwright test --grep-invert @live   # everything else — free and unlimited
npx playwright test --project=contract    # offline: no network at all
npx playwright test --grep @live          # deliberately spend quota`,
  },
  {
    type: 'p',
    text: '**Why there:** the tag rather than a config flag, because the distinction is a property of the *test*, not of the run. A pipeline can then choose per job — the pull-request job runs `--grep-invert @live`, and a nightly job spends the quota once.',
  },
  {
    type: 'note',
    text: "When the quota is gone the live tests skip with a reason rather than failing. That is deliberate: a red build caused by somebody else's usage teaches nobody anything, and a suite people learn to ignore is worse than no suite.",
  },

  { type: 'h2', text: 'Point the CRUD suite at your own API' },
  {
    type: 'steps',
    items: [
      {
        text: 'Add your environment to `src/config/environments.ts`. URLs, prefix, latency budget and the read-only flag — no secrets.',
        code: `sandbox: {\n  name: 'sandbox',\n  apiBaseUrl: 'https://api.sandbox.example.com',\n  apiPrefix: '/v2',\n  verifyTls: true,\n  latencyBudgetMs: 3000,\n  readOnly: false,\n},`,
      },
      {
        text: 'Copy `src/services/object.service.ts`. Declare the schema first and infer the type from it, so the runtime check and the compile-time type cannot disagree.',
        code: `export const OrderSchema = z.object({\n  id: identifier,\n  status: z.enum(['draft', 'paid', 'shipped']),\n  createdAt: isoDateTime,\n}).passthrough();\n\nexport type Order = z.infer<typeof OrderSchema>;`,
      },
      {
        text: '**Establish what the API really does by calling it**, not by reading its documentation. Every quirk in the reference service — a 200 where 201 was expected, timestamps on some responses and not others, a non-standard error shape — was found that way, and each would have broken a schema copied from a specification.',
      },
      {
        text: 'Register the schemas so the contract guard can find them from a real request.',
        code: `registerSchemas([\n  { name: 'order', method: 'GET', pathPattern: '/orders/{id}', status: '2xx', schema: OrderSchema },\n]);`,
      },
      {
        text: 'Add the service to the `api` fixture in `src/fixtures/api.fixture.ts` — one line, and every test can reach it.',
      },
      {
        text: 'Copy the shape of `tests/api/objects.crud.spec.ts`: one composing lifecycle test, several focused ones.',
      },
    ],
  },
  {
    type: 'p',
    text: '**Why that shape:** the lifecycle test proves the operations compose and tells you *that* something broke; the focused tests each assert one behaviour and tell you *what*. Either alone leaves a real gap.',
  },

  { type: 'h2', text: 'Model an API you cannot call freely' },
  {
    type: 'p',
    text: '**Where:** a new module in `src/mocks/`, following `objects.stub.ts`. Worth doing whenever the real dependency is rate-limited, shared, flaky, or simply down half the time.',
  },
  {
    type: 'code',
    caption: 'Register the contract on the stub server, then point a derived client at it',
    text: `export function stubOrdersApi(server: MockServer) {
  const store = new Map<string, StoredOrder>();

  server.stub({
    method: 'POST',
    path: '/orders',
    respond: (request) => { /* … model what the API really does … */ },
  });

  return { store, reset: () => store.clear() };
}`,
  },
  {
    type: 'code',
    caption: 'And drive it with the same service object',
    text: `test.beforeEach(({ mockServer }) => {
  mockServer.reset();
  stubOrdersApi(mockServer);
});

const orders = new OrderService(http.withBaseUrl(mockServer.url), { cleanup });`,
  },
  {
    type: 'ul',
    items: [
      "**Use `withBaseUrl`, not a new client.** Deriving keeps the run's observers — latency, recording, contract guard — attached.",
      "**Reset in `beforeEach`.** The stub server is worker-scoped, so without a reset one test's writes are visible to the next and the suite becomes order-dependent.",
      '**Model the quirks, not the ideal.** A stub that behaves the way you *wish* the API behaved tests nothing.',
      '**Keep a live suite.** A model nobody checks against reality is a fixture that encodes an assumption and then defends it forever.',
    ],
  },
  {
    type: 'rule',
    text: 'The two suites must drive **the same service object**. That is what makes the arrangement work: the stub is not a parallel implementation to keep in step, it is a server the real client talks to.',
  },

  { type: 'h2', text: 'Point the suite at a different environment' },
  { type: 'p', text: '**Where:** `.env`, or `src/config/environments.ts` for a new one.' },
  { type: 'code', caption: 'Switch for one run', text: `TEST_ENV=dev npx playwright test` },
  {
    type: 'p',
    text: 'To add an environment that does not exist yet, add an entry to `ENVIRONMENTS`. It is `as const satisfies Record<string, EnvironmentDefinition>`, so a missing field is a compile error rather than an `undefined` at run time, and the new key becomes a valid `TEST_ENV` automatically because the Zod enum is built from the same object.',
  },
  {
    type: 'p',
    text: '**Why there:** the environment table holds only non-secret values — URLs, prefixes, budgets, the read-only flag — so it is safe in a pull request. Anything credential-shaped is read from the process environment instead.',
  },

  { type: 'h2', text: 'Add an environment variable' },
  {
    type: 'steps',
    items: [
      {
        text: 'Add it to `.env.example` **with a comment explaining what it does**. This file is the only documentation most people will read.',
      },
      {
        text: 'Add it to the schema in `src/config/env.config.ts`, using one of the coercion helpers so a blank value falls back rather than failing.',
        code: `MAX_UPLOAD_MB: integerish(25),\nFEATURE_FLAGS: optionalString(),`,
      },
      {
        text: 'Expose it on the frozen `config` object with a name that reads well at the call site.',
        code: `maxUploadMb: raw.MAX_UPLOAD_MB,`,
      },
      { text: 'If CI needs it, add it to the `env:` block in `.github/workflows/api-tests.yml`.' },
    ],
  },
  {
    type: 'p',
    text: '**Why there:** `env.config.ts` is the only file that touches `process.env`. That makes secret handling reviewable in one place, and turns a misconfiguration into one clear message at start-up instead of a confusing `undefined` deep inside a request.',
  },

  { type: 'h2', text: 'Add a service object for a new resource' },
  {
    type: 'steps',
    items: [
      {
        text: 'Copy `src/services/template.service.ts`. It is written to be copied — every question that comes up on the second service object is answered in it.',
      },
      {
        text: 'Declare the schema first and infer the type from it, so the runtime check and the compile-time type cannot disagree.',
        code: `export const OrderSchema = z.object({\n  id: identifier,\n  reference: z.string(),\n  status: z.enum(['draft', 'paid', 'shipped']),\n  createdAt: isoDateTime,\n});\n\nexport type Order = z.infer<typeof OrderSchema>;`,
      },
      {
        text: 'Register the schema so the contract guard and coverage reporting can find it.',
        code: `registerSchemas([\n  { name: 'order', method: 'GET', pathPattern: '/orders/{id}', status: '2xx', schema: OrderSchema },\n]);`,
      },
      {
        text: 'Set `basePath` and write intention-revealing methods. A creation registers its own cleanup in the same statement.',
        code: `export class OrderService extends BaseService {\n  protected readonly basePath = '/orders';\n\n  async create(overrides: Partial<Order> = {}): Promise<Order> {\n    const response = await this.post().json(buildInvoice(faker, overrides)).expectStatus(201).send();\n    const order = response.parse(OrderSchema, 'order');\n    return this.track(order, \`order \${order.id}\`, () => this.remove(order.id));\n  }\n}`,
      },
      {
        text: 'Add it to the `api` fixture in `src/fixtures/api.fixture.ts`, which makes it available to every test.',
        code: `api: async ({ http, cleanup, log }, use) => {\n  await use({\n    users: new UserService(http, { cleanup, logger: log.child('users') }),\n    orders: new OrderService(http, { cleanup, logger: log.child('orders') }),\n  });\n},`,
      },
    ],
  },
  {
    type: 'p',
    text: "**Why there:** the service is the only thing that knows the endpoint's path and payload shape. Spread that knowledge across tests and a rename becomes a repository-wide search instead of a one-line edit.",
  },

  { type: 'h2', text: 'Add an authentication scheme' },
  { type: 'p', text: '**Where:** a new file in `src/auth/`, implementing `AuthProvider`.' },
  {
    type: 'code',
    caption: 'The whole interface',
    text: `export class MutualTlsAuth implements AuthProvider {
  readonly name = 'mtls';

  async headers(): Promise<HeaderMap> {
    return { 'x-client-cert-fingerprint': await this.fingerprint() };
  }

  invalidate(): void {
    this.cached = undefined;
  }
}`,
  },
  {
    type: 'p',
    text: 'If it should be chosen automatically, add it to `defaultAuthProvider` in `src/fixtures/api.fixture.ts` — the order in that function is the precedence.',
  },
  {
    type: 'p',
    text: '**Why there:** `headers()` is called per request rather than once, which is what allows a provider to refresh a token transparently. Anything that needs the request itself — request signing — is a signer rather than a provider; see `HmacSigner`.',
  },

  { type: 'h2', text: 'Add a custom assertion' },
  { type: 'p', text: 'Two homes, and the choice matters.' },
  {
    type: 'table',
    head: ['Put it in…', 'When'],
    rows: [
      [
        '`ApiResponse` (`src/core/api.response.ts`)',
        'It is about a response, and reads better chained: `response.expectOk().expectSecurityHeaders()`',
      ],
      [
        '`custom-matchers.ts`',
        'It needs to be negatable, soft, or applied to something that is not a response: `expect(payload).toMatchPayload(baseline)`',
      ],
    ],
  },
  {
    type: 'code',
    caption: 'A matcher — the return type is what gives it its types at every call site',
    text: `toHaveCursor(response: ApiResponse, expected?: string): MatcherResult {
  const cursor = response.path<string>('nextCursor');
  const pass = expected === undefined ? cursor !== undefined : cursor === expected;
  return {
    pass,
    message: () =>
      pass
        ? \`Expected no cursor, found "\${cursor}".\`
        : \`Expected cursor \${expected ?? '(any)'}, found \${cursor ?? '(none)'}.\${context(response)}\`,
  };
}`,
  },
  {
    type: 'p',
    text: '**Why there:** matchers are exported through `expect.extend`, which returns a *typed* `expect`. Adding one to that object is all that is needed — there is no separate type declaration to keep in step.',
  },

  { type: 'h2', text: 'Stub a dependency you cannot control' },
  { type: 'p', text: '**Where:** the `mockServer` fixture, in the test itself.' },
  {
    type: 'code',
    caption: 'Failure, latency and recovery, all on demand',
    text: `test('retries a flaky dependency', async ({ http, mockServer }) => {
  mockServer.reset();
  mockServer.flaky('/payments', 2, { status: 'settled' });

  const response = await http.withBaseUrl(mockServer.url).post('/payments').retries(3).send();

  response.expectOk();
  expect(response.timing.attempts).toBe(3);
  expect(mockServer.callCount('/payments')).toBe(3);
});`,
  },
  {
    type: 'p',
    text: '**Why there:** the stub proves the client really recovers, because the endpoint really does. A mocked-out client that always succeeds proves nothing at all.',
  },

  { type: 'h2', text: 'Make a test wait for eventual consistency' },
  { type: 'p', text: "**Where:** Playwright's `expect.poll()`. Never a sleep." },
  {
    type: 'code',
    caption: 'Poll for the thing you are actually waiting for',
    text: `await expect
  .poll(() => api.orders.find(id), {
    message: \`order \${id} never appeared in the index\`,
    timeout: TIMEOUTS.POLL_TIMEOUT,
  })
  .toBeDefined();`,
  },
  {
    type: 'rule',
    text: 'A fixed sleep is too short on a slow day and too long on every other day, and it hides the fact that the wait exists at all. `expect.poll()` states the condition, retries it until the assertion passes, and fails with a message naming what never happened.',
  },

  { type: 'h2', text: 'Change a timeout' },
  {
    type: 'p',
    text: '**Where:** `src/config/timeouts.ts` for a budget the whole suite shares; `.timeout(ms)` on the builder for one request.',
  },
  {
    type: 'p',
    text: '**Why there:** named budgets mean a slow environment is retuned in one edit, and every number in the suite carries an explanation of what it is waiting for. A raw `30000` in a test tells the next reader nothing.',
  },
  {
    type: 'warn',
    text: 'Raising a timeout to make a flaky test pass converts a fast failure into a slow one. Find out what is actually slow first — the latency collector attaches a per-route report to every test.',
  },

  { type: 'h2', text: 'Validate against a published OpenAPI document' },
  {
    type: 'steps',
    items: [
      {
        text: 'Bundle the specification into a single file — external `$ref`s are deliberately not resolved — and save it as `src/data/openapi.json`.',
        code: `npx @redocly/cli bundle openapi.yaml -o src/data/openapi.json`,
      },
      { text: 'The `contract` fixture picks it up automatically; no wiring is needed.' },
      {
        text: 'Assert conformance with the matcher.',
        code: `test('matches the published contract', async ({ http, contract }) => {\n  test.skip(!contract, 'No OpenAPI document in src/data/');\n  const response = await http.get('/users/1').send();\n  expect(response).toSatisfyContract(contract);\n});`,
      },
      {
        text: 'For coverage, ask which documented operations the suite never exercised.',
        code: `const gaps = contract.uncovered(exercisedCalls);`,
      },
    ],
  },
  {
    type: 'p',
    text: '**Why there:** a hand-written schema records what the test author believed. The document records what the API *promised*. Only the second catches the case where the API and its published contract have drifted and every consumer who trusted the document is broken.',
  },

  { type: 'h2', text: 'Add a latency budget' },
  {
    type: 'code',
    caption: 'Two levels',
    text: `// Per request, in any test:
response.expectFasterThan(400);

// Per route, across many samples, in tests/performance/:
const summary = await sample(() => api.users.list(), { runs: 30, warmup: 3 });
expect(summary.p95).toBeLessThan(800);`,
  },
  {
    type: 'p',
    text: 'The environment-wide default lives in `latencyBudgetMs` on the environment, or `LATENCY_BUDGET_MS`; `expectWithinLatencyBudget()` uses it.',
  },
  {
    type: 'p',
    text: '**Why there:** percentile checks belong in the `performance` project because it runs with a single worker. Measured under eight parallel workers, a p95 describes the load the suite generates, not the endpoint.',
  },

  { type: 'h2', text: 'Test authorisation properly' },
  {
    type: 'code',
    caption: 'Generate the grid rather than the cases you thought of',
    text: `const matrix = buildAccessMatrix(
  ['admin', 'standard', 'readonly'],
  [
    { method: 'GET', path: '/users/{id}', allowed: ['admin', 'standard', 'readonly'] },
    { method: 'DELETE', path: '/users/{id}', allowed: ['admin'] },
  ],
);

for (const cell of matrix) {
  test(\`\${cell.role} \${cell.method} \${cell.path} @security\`, async ({ httpAs }) => {
    const response = await httpAs(cell.role).request(cell.method, cell.path).param('id', seedId).send();
    expect(judgeAccess(cell, response.status), formatFindings([...])).toBeUndefined();
  });
}`,
  },
  {
    type: 'p',
    text: '**Why there:** authorisation defects are defects of *omission* — the endpoint nobody remembered to protect. Generating the whole grid is the only way to catch that class reliably; writing the cases somebody thought of catches exactly the cases somebody thought of.',
  },

  { type: 'h2', text: 'Record a run and replay it offline' },
  {
    type: 'code',
    caption: 'Record once, then run with no network',
    text: `# Record against a real environment
TEST_ENV=staging npx playwright test --project=api

# The recorder saves failures automatically; save everything by hand:
recorder.save('src/data/recordings/users.json');`,
  },
  {
    type: 'code',
    caption: 'Replay through the stub server',
    text: `const recording = ExchangeRecorder.load('src/data/recordings/users.json');
for (const stub of ExchangeRecorder.toStubs(recording)) mockServer.stub(stub);`,
  },
  {
    type: 'warn',
    text: 'Recordings are redacted on the way out, but check one before committing it. A recording containing a live token is a credential in the repository, and no amount of later deletion fixes that.',
  },

  { type: 'h2', text: 'Add a project to the run' },
  {
    type: 'p',
    text: '**Where:** the `projects` array in `playwright.config.ts`, plus a folder under `tests/` and a script in `package.json`.',
  },
  {
    type: 'code',
    caption: 'A smoke project that runs before a deploy',
    text: `{
  name: 'smoke',
  testDir: './tests/api',
  grep: /@smoke/,
  dependencies: ['setup'],
  retries: 0,
  workers: 4,
},`,
  },
  {
    type: 'p',
    text: '**Why there:** a project is the unit that carries its own settings and its place in the dependency order. Expressing "log in first" as a dependency rather than a global variable is what keeps it visible in the report and retryable like anything else.',
  },

  { type: 'h2', text: 'Add a reporter' },
  {
    type: 'p',
    text: '**Where:** the `reporter` array in `playwright.config.ts`; a custom one goes in `src/reporters/`.',
  },
  {
    type: 'p',
    text: 'Make anything heavy conditional, the way Allure and the blob reporter already are — a reporter that runs on every local run is a tax on every local run.',
  },
  {
    type: 'note',
    text: 'A reporter passed on the command line **replaces** the list in the config. `--reporter=list` is useful for a quick loop, but it also means no `summary.json`, no JUnit and no HTML report for that run.',
  },

  { type: 'h2', text: 'Change how a failure reports' },
  {
    type: 'p',
    text: '**Where:** the private `assert` method in `src/core/api.response.ts`. Every built-in assertion funnels through it, so one edit changes the anatomy of every failure message in the suite.',
  },
  {
    type: 'p',
    text: '**Why there:** consistency is the point. A reader who has seen one failure should be able to read every failure without re-orienting.',
  },

  { type: 'h2', text: 'Support a new body encoding' },
  {
    type: 'steps',
    items: [
      {
        text: 'Add the kind to `BodyKind` in `src/types/index.ts`. The compiler will now list every place that has to handle it — three exhaustive switches.',
      },
      {
        text: 'Add the builder method in `src/core/request.builder.ts`, calling `assertBodyUnset` so two bodies cannot be set at once.',
      },
      { text: 'Add the transport case in `HttpClient.transport`.' },
      { text: 'Add the log rendering in `describeRequestBody` in `src/core/api.response.ts`.' },
    ],
  },
  {
    type: 'p',
    text: '**Why there:** `BodyKind` is a discriminated union precisely so this is a compiler-guided change rather than a search. Nothing is missed because nothing *can* be missed.',
  },

  { type: 'h2', text: 'Add a fixture' },
  {
    type: 'p',
    text: '**Where:** `src/fixtures/api.fixture.ts` — declare the type in `ApiFixtures` or `WorkerFixtures`, then implement it.',
  },
  {
    type: 'code',
    caption: 'Set up before, clean up after; `use` is the boundary',
    text: `tenant: async ({ http }, use) => {
  const created = await http.post('/tenants').json({ name: uniqueId('tenant') }).send();
  const id = created.path<string>('id');

  await use(id);                                   // the test runs here

  await http.delete(\`/tenants/\${id}\`).send();      // runs even if the test failed
},`,
  },
  {
    type: 'warn',
    text: 'A fixture that depends on another fixture that depends back on it deadlocks with an unhelpful error. If two need each other, split the shared part into a third — `createConsoleCapture`-style — rather than trying to break the cycle in place.',
  },

  { type: 'h2', text: 'Debug one failing test' },
  {
    type: 'code',
    caption: 'Escalating detail',
    text: `# Everything the framework knows
LOG_LEVEL=debug LOG_BODIES=true npx playwright test path/to/spec.ts --workers=1

# Replay the run in the trace viewer
npx playwright show-trace test-results/<test-folder>/trace.zip

# Step through in a debugger — the VS Code launch config does this too
PWDEBUG=1 npx playwright test path/to/spec.ts --debug`,
  },
  {
    type: 'p',
    text: 'A failed test also leaves `reports/recordings/<test>.json`, containing every exchange with credentials redacted. That is usually enough to diagnose without re-running anything.',
  },

  { type: 'h2', text: 'Run against a local service' },
  {
    type: 'code',
    text: `TEST_ENV=local npx playwright test
# or override just the URL
API_BASE_URL=http://localhost:8080 npx playwright test`,
  },
  {
    type: 'p',
    text: 'The `local` environment sets `verifyTls: false`, which is the one place a self-signed certificate is acceptable.',
  },

  { type: 'h2', text: 'Add a page to this documentation' },
  {
    type: 'steps',
    items: [
      {
        text: 'Add the prose to `scripts/docs/content/guides.mjs` as an exported array of blocks.',
      },
      { text: 'Add an entry to `PAGES` in `scripts/docs/build-docs.mjs`.' },
      { text: 'Rebuild.', code: `npm run docs` },
    ],
  },
  {
    type: 'p',
    text: 'Documenting a **new source file** is not optional: the build fails with the path and refuses to produce a site until an entry exists in `scripts/docs/content/`. That gate is the reason this document can be trusted.',
  },

  { type: 'h2', text: 'Rotate a credential' },
  {
    type: 'ol',
    items: [
      'Update the repository secret in GitHub — never a file in the repository.',
      'Update your own `.env`, which is git-ignored.',
      'Delete `storage/*.json`. Those hold captured sessions and tokens for the *old* credential and will keep being replayed until they are removed.',
      'Re-run. `auth.setup.ts` captures a new session on the next run.',
    ],
  },
  {
    type: 'warn',
    text: 'A `storage/` file is a live credential. Committing one is equivalent to committing a password, which is why the whole directory is git-ignored and the files are written with mode `0600`.',
  },
];

/* ------------------------------------------------------------------ */

export const conventions = [
  {
    type: 'p',
    text: 'Rules the codebase already follows. They are worth stating because most of them are enforced by tooling, and a reviewer should know which is which.',
  },

  { type: 'h2', text: 'Non-negotiables' },
  {
    type: 'rule',
    text: '**Import `test` and `expect` from `@fixtures`, never from `@playwright/test`.** Importing Playwright directly gives you a `test` with no cleanup registry, no contract guard and none of the custom matchers — and nothing will tell you. This is the single most expensive mistake available in the repository.',
  },
  {
    type: 'rule',
    text: '**Never read `process.env` outside `src/config/env.config.ts`.** One reader is what makes secret handling reviewable and turns a misconfiguration into one clear message.',
  },
  {
    type: 'rule',
    text: '**Never sleep.** Use `waitFor`. A fixed delay is too short on a slow day, too long on every other, and silent about what it is waiting for.',
  },
  {
    type: 'rule',
    text: '**Never retry an assertion.** `retry()` is for transient work — a flaky sandbox, a service restarting. Retrying until a test passes is how a real defect reaches production.',
  },
  {
    type: 'rule',
    text: "**Never commit a credential.** `.env.example` is a template with placeholders. `.env*` and `storage/*.json` are git-ignored. CI reads secrets from the repository's secret store.",
  },
  {
    type: 'rule',
    text: '**Register cleanup at the point of creation.** In the same statement, in the service method. A resource created without a registered deletion is a leak that somebody else finds months later.',
  },

  { type: 'h2', text: 'Naming' },
  {
    type: 'table',
    head: ['Thing', 'Convention', 'Example'],
    rows: [
      ['Spec file', '`<resource>.<behaviour>.spec.ts`', '`users.pagination.spec.ts`'],
      [
        'Service object',
        '`<Resource>Service`, in `<resource>.service.ts`',
        '`OrderService` in `order.service.ts`',
      ],
      ['Schema', 'PascalCase, suffixed `Schema`', '`OrderSchema`'],
      ['Type from a schema', 'PascalCase, inferred', '`type Order = z.infer<typeof OrderSchema>`'],
      [
        'Auth provider',
        '`<Scheme>Auth`, in `<scheme>.auth.ts`',
        '`OAuth2Auth` in `oauth2.auth.ts`',
      ],
      ['Utility module', '`<domain>.utils.ts`', '`pagination.utils.ts`'],
      ['Fixture', 'camelCase noun, no `get` prefix', '`mockServer`, not `getMockServer`'],
      ['Factory', '`build<Thing>`', '`buildOrder`'],
      ['Assertion method', '`expect<Property>`', '`expectSecurityHeaders`'],
      ['Matcher', '`to<Property>`', '`toSatisfyContract`'],
    ],
  },

  { type: 'h2', text: 'Tags' },
  {
    type: 'p',
    text: 'Every test carries at least two: a suite tag and a domain tag. Tags are how a pipeline selects a subset without anybody maintaining a list of file paths.',
  },
  {
    type: 'table',
    head: ['Tag', 'Means'],
    rows: [
      [
        '`@smoke`',
        'Must pass before a deploy. Keep it to a handful — a smoke suite that takes ten minutes is not a smoke suite.',
      ],
      ['`@regression`', 'The main body of the suite.'],
      ['`@contract`', 'Asserts conformance to a published schema or document.'],
      ['`@security`', 'Authorisation, input handling, response hygiene.'],
      [
        '`@live`',
        '**Spends real-API quota.** `--grep-invert @live` runs everything else for free.',
      ],
      ['`@slow`', 'Legitimately slow — a report, a bulk import. Excluded from the fast loop.'],
      ['`@read-only`', 'Safe against a read-only environment; issues no mutating verb.'],
      ['`@flaky`', 'Known-unstable and being fixed. Should always have an owner and a ticket.'],
      ['`@users`, `@orders`, …', 'The domain area, one per test.'],
    ],
  },
  {
    type: 'code',
    caption: 'Selecting',
    text: `npx playwright test --grep @smoke
npx playwright test --grep "@users.*@regression"
npx playwright test --grep-invert "@slow|@flaky"
npx playwright test --grep-invert @live       # spend no API quota`,
  },

  { type: 'h2', text: 'Writing a test' },
  {
    type: 'code',
    caption: 'The shape',
    text: `import { test, expect } from '../../src/fixtures';

test.describe('user creation', () => {
  test('rejects a duplicate email @regression @users', async ({ api, http, data }) => {
    // Arrange — through the service, which registers its own cleanup.
    const existing = await api.users.create();

    // Act — the raw response, because the assertion is about the status.
    const response = await http
      .post('/users')
      .json({ email: existing.email, firstName: data.person.firstName() })
      .send();

    // Assert — one behaviour, with the reason visible in the message.
    response.expectStatus(409);
    expect(response).toMatchSchema(problemDetails, 'problem-details');
    expect(response.path('detail')).toContain('email');
  });
});`,
  },
  {
    type: 'ul',
    items: [
      '**One behaviour per test.** A test asserting three unrelated things reports the first failure and hides the other two.',
      '**Arrange through services, assert in the test.** Setup that goes through the service is setup that stays correct when the API changes.',
      '**Name the test as the behaviour, not the mechanics.** "rejects a duplicate email", not "POST /users returns 409".',
      '**Prefer `response.parse(Schema)` over `response.json()`.** It makes the shape a checked fact, and everything after it is fully typed.',
      '**Use `soft()` when asserting many properties of one payload.** One run then reports every violation instead of only the first.',
    ],
  },

  { type: 'h2', text: 'Anti-patterns' },
  {
    type: 'table',
    head: ['Instead of…', 'Do this', 'Because'],
    rows: [
      [
        '`await sleep(2000)`',
        '`await waitFor(() => …)`',
        'States the condition, fails with a useful message, and is as fast as the system allows',
      ],
      [
        "`http.get('/v1/users/42')` in a test",
        '`api.users.find(42)`',
        'A rename becomes one edit rather than a repository search',
      ],
      [
        '`const user = response.json() as User`',
        '`const user = response.parse(UserSchema)`',
        'A cast is a wish; a parse is a check',
      ],
      [
        '`expect(response.status).toBe(200)`',
        '`response.expectOk()`',
        'The failure message carries the request, timing and body',
      ],
      [
        'A hard-coded `test@example.com`',
        '`uniqueId()`',
        'Survives a uniqueness constraint and a second run',
      ],
      [
        'Asserting on page one of a list',
        '`followOffset` / `followLinkHeader`',
        'Page two is where pagination bugs live',
      ],
      [
        '`test.describe.serial` to fix ordering',
        'Independent tests with their own setup',
        'Serial suites hide the coupling instead of removing it',
      ],
      [
        'Sharing state in a worker fixture',
        'Test-scoped fixtures',
        'Shared mutable state makes failures depend on worker count',
      ],
      [
        '`expect(errors.length).toBe(0)` on a GraphQL call',
        '`result.expectNoErrors()`',
        'GraphQL answers 200 with the failure in the body; a status assertion passes anyway',
      ],
    ],
  },

  { type: 'h2', text: 'TypeScript' },
  {
    type: 'ul',
    items: [
      '`strict` and `noUncheckedIndexedAccess` are on. An indexed read is `T | undefined` and must be handled — that rule alone prevents a whole class of run-time errors.',
      '`import type` for type-only imports. Enforced by `consistent-type-imports`.',
      'Explicit return types on exported functions. The compiler infers them, but a reader should not have to.',
      'No `any`. Use `unknown` and narrow — that is what `UnknownRecord` and the type guards are for.',
      'Path aliases (`@core/*`, `@utils/*`) over long relative chains.',
    ],
  },

  { type: 'h2', text: 'Comments' },
  {
    type: 'p',
    text: 'Comment the **why**, never the what. A comment that restates the code is a comment that will drift out of step with it. Every non-obvious decision in this framework carries a comment explaining the alternative that was rejected and the reason — that is the comment that is still useful in two years.',
  },

  { type: 'h2', text: 'Review checklist' },
  {
    type: 'ul',
    items: [
      'Does it import from `@fixtures`?',
      'Is it tagged with a suite tag and a domain tag?',
      'Does everything it creates get cleaned up?',
      'Is the data generated rather than hard-coded?',
      'Is there a sleep? A retried assertion? A cast where a parse would do?',
      'Does a new endpoint have a registered schema?',
      'Would the failure message tell somebody at 3am what broke?',
      'Does a new source file have a documentation entry? (`npm run docs` will tell you.)',
      'Does `npm run validate` pass?',
    ],
  },
];

/* ------------------------------------------------------------------ */

export const troubleshooting = [
  {
    type: 'p',
    text: 'Failure modes, what actually causes them, and how to find out. Ordered by how often they come up.',
  },

  { type: 'h2', text: 'Start-up failures' },
  {
    type: 'table',
    head: ['Message', 'Cause', 'Fix'],
    rows: [
      [
        '`Invalid environment configuration`',
        'A variable is missing or malformed. The message lists each one by name.',
        'Copy `.env.example` to `.env` and fill in the named variables.',
      ],
      [
        '`Cannot reach <url>`',
        '`globalSetup` could not open a connection at all.',
        'Check `TEST_ENV`, the URL in `environments.ts`, and whether the target needs a VPN or tunnel.',
      ],
      [
        '`No credentials for role "admin"`',
        "`getUser('admin')` was called with no `ADMIN_USERNAME`/`ADMIN_PASSWORD`.",
        "Set both, or use `hasUser('admin')` to skip rather than fail.",
      ],
      [
        '405 with "daily request limit"',
        'The `api.restful-api.dev` anonymous quota (50 requests / 24 h) is spent. Not a defect and not a transport fault.',
        'Nothing — the live suite skips and `tests/contract/objects.stubbed.spec.ts` covers the same lifecycle offline. Use `--grep-invert @live` to skip it deliberately.',
      ],
      [
        '`Refusing to send POST …: read-only`',
        'The environment is marked `readOnly` and a test tried to write.',
        'Point `TEST_ENV` at a writable environment, or tag the test `@read-only` and make it read-only in fact.',
      ],
    ],
  },

  { type: 'h2', text: 'Authentication' },
  {
    type: 'table',
    head: ['Symptom', 'Likely cause'],
    rows: [
      [
        '`invalid_client` from the token endpoint',
        "Client credentials sent in the wrong place. Providers differ; try `clientAuth: 'body'`.",
      ],
      [
        '401s that start partway through a run',
        'A token expired mid-run. The store renews 30s early — if the API issues very short tokens, that skew may need raising.',
      ],
      [
        '401 on the first request only',
        'The auth provider is producing headers, but the request set its own `authorization`, which always wins.',
      ],
      [
        'Session auth logs in on every request',
        'The login response set no cookie matching `cookieNames`. The error lists which cookies actually arrived.',
      ],
      [
        '`storage/` session keeps being reused after a password change',
        'Delete `storage/*.json`. The captured session outlives the credential that created it.',
      ],
    ],
  },

  { type: 'h2', text: 'Flakiness' },
  {
    type: 'p',
    text: 'An API suite has four common sources of flake, and they have different fixes. Guessing between them is what makes flakiness feel unfixable.',
  },
  {
    type: 'table',
    head: ['Source', 'How it looks', 'Fix'],
    rows: [
      [
        'Eventual consistency',
        'A resource is created, then a read of it 404s — intermittently',
        '`waitFor` the read, not a sleep',
      ],
      [
        'Shared state between tests',
        'Passes at `--workers=1`, fails at `--workers=8`, and the failure moves',
        'Generate unique data; move mutable state to test scope',
      ],
      [
        'Rate limiting',
        'Bursts of 429, often at the start of a run',
        'Lower `workers`, use `mapWithConcurrency` when seeding, and let the client honour `Retry-After`',
      ],
      [
        'Genuine instability in the target',
        'Random 502/503 unrelated to the test',
        'Retries already handle it — but raise it with the service owner; a suite is a good early-warning system',
      ],
    ],
  },
  {
    type: 'code',
    caption: 'Confirming it is ordering, not the test',
    text: `npx playwright test --workers=1        # passes → shared state or rate limiting
npx playwright test --repeat-each=5   # fails sometimes → genuinely non-deterministic`,
  },

  { type: 'h2', text: 'Contract failures' },
  {
    type: 'table',
    head: ['Message', 'Meaning'],
    rows: [
      [
        '`violates the registered schema "x"`',
        'The contract guard fired. The API returned something the schema does not allow — either the API changed, or the schema is wrong.',
      ],
      [
        '`documents no response for status 4xx`',
        'The OpenAPI document has no entry for that status. Usually the document is incomplete, not the API.',
      ],
      [
        '`External $ref … is not supported`',
        'The specification references another file. Bundle it first.',
      ],
      [
        '`No operation documented for GET /x`',
        'The suite is calling an endpoint the document does not describe — a real finding, not a tooling problem.',
      ],
    ],
  },
  {
    type: 'note',
    text: "A contract failure is **never** retried. That is structural: the response listeners run outside the client's retry catch, precisely so a schema violation appears once with a clear message rather than three times.",
  },

  { type: 'h2', text: 'Streaming' },
  {
    type: 'table',
    head: ['Symptom', 'Cause'],
    rows: [
      [
        'A WebSocket test misses the reply it was waiting for',
        'It should not — the client buffers from connect. If it still happens, the predicate does not match the frame; log `client.messages`.',
      ],
      [
        'A test hangs at the end and the worker never exits',
        'An SSE stream or socket was left open. Use the `sse` and `socket` fixtures, which close everything on teardown.',
      ],
      [
        'SSE events never arrive',
        'A proxy is buffering the stream. Confirm with `curl -N`; the framework cannot fix a proxy.',
      ],
      [
        'Keep-alive comments arrive as events',
        'They should not — a line starting with `:` is a comment and is dropped by `parseFrame`. If yours differ, the server is not sending valid framing.',
      ],
    ],
  },

  { type: 'h2', text: 'Performance results that do not make sense' },
  {
    type: 'ul',
    items: [
      '**A p95 much worse than the p50** usually means the suite is competing with itself. Performance tests run at `workers: 1` for this reason; check the project actually ran under it.',
      '**The first sample is always slowest.** That is connection setup and JIT. `sample()` discards warm-up runs by default.',
      '**Every route has one sample.** `routeOf` collapses identifiers, but only the common shapes. Add yours to it if the routes look like `/users/abc123def`.',
    ],
  },

  { type: 'h2', text: 'Diagnostic tools' },
  {
    type: 'table',
    head: ['Tool', 'Command', 'Shows'],
    rows: [
      [
        'Debug logs',
        '`LOG_LEVEL=debug LOG_BODIES=true`',
        'Every request, response, retry and backoff decision',
      ],
      [
        'Trace viewer',
        '`npx playwright show-trace <trace.zip>`',
        'Every request and response, replayable, with timings',
      ],
      [
        'Recording',
        '`reports/recordings/<test>.json`',
        'The full exchange for a failed test, redacted',
      ],
      ['Latency report', 'attached to every test', 'Per-route p50/p90/p95/p99'],
      [
        'Run summary',
        '`reports/summary.json`',
        'Totals, pass rate, the ten slowest tests, every failure',
      ],
      [
        'Stub-server log',
        '`mockServer.requests`',
        'Exactly what the client sent, including headers and body',
      ],
    ],
  },

  { type: 'h2', text: 'Tooling' },
  {
    type: 'table',
    head: ['Symptom', 'Fix'],
    rows: [
      [
        '`program.parse` is undefined, or a dependency exports nothing',
        'A pruned or corrupted install — `npm uninstall` can remove a transitive dependency Playwright needs. `rm -rf node_modules && npm ci`.',
      ],
      [
        'Modules load with empty exports after a clean install',
        'A poisoned Node compile cache. Delete `$TMPDIR/node-compile-cache`; it regenerates.',
      ],
      [
        'No `summary.json`, no HTML report after a run',
        "`--reporter=` on the command line replaces the config's list. Drop it.",
      ],
      [
        '`npm run docs` fails naming a file',
        'That file has no documentation entry. Add one in `scripts/docs/content/`. This is the gate working.',
      ],
      [
        'ESLint reports a rule you disagree with',
        'Change it in `eslint.config.mjs`, with a comment explaining why — as the existing exceptions do.',
      ],
      [
        'Most tests skip rather than run',
        'Either the API quota is spent, or no credentials are configured for the `setup` project. Both are designed to skip rather than fail; the skip reason says which.',
      ],
    ],
  },
];

/* ------------------------------------------------------------------ */

export const glossary = [
  {
    type: 'p',
    text: 'Terms used in the code and in this document, in the sense this framework uses them.',
  },
  {
    type: 'table',
    head: ['Term', 'Meaning here'],
    rows: [
      [
        '**Service object**',
        "A class that owns one resource's paths and payloads and exposes domain methods. The API equivalent of a page object.",
      ],
      [
        '**Fixture**',
        'A Playwright-managed dependency, set up before a test and torn down after. Constructed only if the test names it.',
      ],
      [
        '**Worker scope**',
        'A fixture built once per worker process and shared by every test in it.',
      ],
      [
        '**Snapshot**',
        'The complete captured response — status, headers, body buffer, timing — from which `ApiResponse` is built.',
      ],
      [
        '**Verdict**',
        'A response the API meant to send: a 422, a 403. Never retried, as distinct from a transport fault.',
      ],
      [
        '**Transport fault**',
        'A failure before any response arrived: DNS, TLS, reset, timeout. Retried with backoff.',
      ],
      [
        '**Contract guard**',
        'The automatic fixture that validates every response against its registered schema when `STRICT_CONTRACTS` is on.',
      ],
      [
        '**Schema registry**',
        'The map from method + path pattern + status to a schema, which is what lets the guard find the right one.',
      ],
      [
        '**Idempotency key**',
        'A header promising the server that a repeated request is the same request — what makes retrying a POST safe.',
      ],
      [
        '**Eventual consistency**',
        'A write that is acknowledged before it is visible everywhere. The main cause of flaky API tests.',
      ],
      [
        '**Read-only environment**',
        'One flagged in `environments.ts` where the client refuses to send any mutating verb.',
      ],
      [
        '**Problem details**',
        'RFC 9457, the standard error body: `type`, `title`, `status`, `detail`, `instance`.',
      ],
      [
        '**Cursor pagination**',
        'Paging by an opaque token rather than an offset, so results stay stable while the underlying data changes.',
      ],
      [
        '**Link header**',
        "RFC 8288 pagination, where the server sends the next page's URL and the client needs no knowledge of its parameters.",
      ],
      [
        '**SSE**',
        'Server-Sent Events: a one-way `text/event-stream` where events are `field: value` lines separated by blank lines.',
      ],
      [
        '**NDJSON**',
        'Newline-delimited JSON — one complete JSON value per line, used for streaming and bulk export.',
      ],
      [
        '**Shape**',
        'Which paths exist in a payload and of what type, ignoring values. What a baseline comparison should actually compare.',
      ],
      [
        '**Breaking change**',
        'In shape terms: a field removed, or a field whose type changed. Adding a field is not one.',
      ],
      [
        '**Access matrix**',
        'The full grid of role × operation with the expected outcome — generated, not hand-written, so omissions are caught.',
      ],
      [
        '**Redaction**',
        'Replacing a credential with a correlatable stub before it reaches a log, a report or a recording.',
      ],
      [
        '**Blob report**',
        "Playwright's intermediate per-shard report format, merged into one HTML report after a sharded run.",
      ],
      [
        '**Executable model**',
        "An in-memory implementation of a real API's contract, driven by the same service object, so exhaustive coverage can run offline and unlimited.",
      ],
      [
        '**Echo server**',
        'A service that returns exactly what it received. Neutral in a way a stub is not, because a stub shares its author with the client it is testing.',
      ],
      [
        '**Quota refusal**',
        'A rate-limit response that is neither a defect nor a transport fault. Detected and skipped, because failing on it teaches people to ignore the suite.',
      ],
      [
        '**Flaky**',
        'Here: passed only after a retry. Counted separately from passed by the summary reporter, so retries cannot hide rot.',
      ],
    ],
  },
];
